# Attraktiva Catalog PWA

Simple furniture catalog PWA built with React and Vite. Products are loaded from `public/products.json` and images are served from external URLs.

## Scripts

- `npm run dev` - start development server
- `npm run build` - build for production
- `npm run preview` - preview production build
- `npm test` - run tests
